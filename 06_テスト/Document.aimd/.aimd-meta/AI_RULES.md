<!-- aimd-meta:version 1 -->
# AIMD ルール

- 末端 Markdown ファイル名は `XX_名前.md` 形式を維持する。
- 目次はルート直下の `01_目次.md` を使用し、全ての末端 `.md` を掲載する。
- 画像はルートまたは章直下の `99_Image` 配下に配置する。
- `.aimd-meta` 配下のファイルは AI 管理領域として扱う。
